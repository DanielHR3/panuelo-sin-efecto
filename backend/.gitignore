node_modules

# Build output
/dist

# Keep environment variables out of version control (se versiona .env.example)
.env

/generated/prisma

# SQLite dev databases must not be committed (contain local/seeded data)
*.db
*.db-journal
